# nvvec Benchmark Report(第一代)

日期:2026-07-27 | 代码:本仓库 main 分支 | 全部数字可用文中命令复现

## 测试环境

| 平台 | 用途 | 规格 |
|---|---|---|
| A. 阿里云 i 系列按量实例 | **正式数字** | 4C Xeon / 30GB / 894GB 本地 NVMe(fio 实测:QD1 68μs,饱和 **500k IOPS**),Ubuntu 24.04,内核 6.8 |
| B. WSL2(开发机) | 机制验证 | 16 线程,虚拟盘(~125μs/IO,单 ring ~80k IOPS 上限) |
| C. 阿里云高效云盘 ECS | 公式验证 | 2C,盘 2.2k IOPS 封顶 |

数据集:SIFT1M(1M × 128 维 f32),recall@10 对官方 ground truth。

## 核心结果(平台 A,真 NVMe,O_DIRECT)

| 配置 | 线程 | 路由内存 | recall@10 | QPS | 设备 IOPS |
|---|---|---|---|---|---|
| 同步 pread 基线(w=1,冷缓存) | 4 | 512 MB | 0.947 | 1,025 | ~88k |
| 流水线 exact,c=16 | **1** | 512 MB | 0.955 | 1,378 | 132k |
| 流水线 sq8,c=64 | **1** | **128 MB** | 0.955 | 1,001 | 96k |
| 流水线 sq8,ef=120 | 1 | 128 MB | **0.971** | 752 | 101k |
| **流水线 sq8 × 4 进程** | 4 | 128 MB | 0.951 | **2,532** | **242k** |

**验收判定**:

- ✅ **每线程效率 5.4×**:单调度线程流水线(1,378 QPS)= 同步模式单线程份额(256 QPS)的 5.4 倍——io_uring 深队列假设成立
- ✅ **量化内存 1/4、召回损失 ~0**(0.9550→0.9549,块内精排把量化误差挡在返回结果之外)
- ⚠️ **IOPS 利用率 48%**(242k/500k,目标 60%):瓶颈是 4 核 CPU 全部饱和(调度线程 + 内核完成处理抢核),不是 I/O 架构——同代码在页缓存模式下单线程可推 2.6k+ QPS。**更多核的机器上此数字随核数增长**
- 对照:同硬件(4 核)内存版 HNSW 估算 ~4k QPS @ 512MB+ 常驻 → 磁盘版做到 **~60% 性能 @ 1/4 路由内存**,且全精度向量完全不占内存

## 机制验证链(每个环节独立可证伪)

1. **每跳一次 I/O**:reads == hops 精确相等(M2,测试断言)
2. **QPS = 盘 IOPS ÷ 每查询读数**:云盘 2.2k IOPS ÷ 86 读 = 25.6,实测 29(平台 C)
3. **W 束宽摊薄延迟**:O_DIRECT 下 w=1→8 得 5.8×(平台 B)
4. **跨查询流水线**:c=1→64 得 5.5×,单变量实验(平台 B);真 NVMe c=1→16 得 3.6×(平台 A)
5. **量化路由无损返回**:返回距离与真实 L2 逐位相等(测试断言);recall 差 ≤0.1pt

## BIGANN-10M(10M × 128,平台 A)

建图 99 分钟(4 核,R=32/L=100,平均度 31.9);磁盘索引 6.9GB;SQ8 训练 8.9s。

| 配置 | 线程 | 路由内存 | recall@10 | QPS |
|---|---|---|---|---|
| 内存版 Vamana(对照) | 4 | 6.4 GB(向量+图) | 0.912 | 1,514 |
| 流水线 exact,c=16 | 1 | 5.1 GB | 0.916 | 968 |
| 流水线 sq8,c=32 | 1 | **1.28 GB** | 0.917 | 753 |
| 流水线 sq8,ef=120 | 1 | 1.28 GB | **0.943** | 493 |
| **流水线 sq8 × 2 进程** | 2 | 1.28 GB | 0.917 | **1,459**(152k IOPS) |

**规模扩展观察(1M → 10M,数据量 10×)**:

- 每查询读数 96 → 104(**仅 +8%,对数增长**)——图检索的核心优势在磁盘形态下保持
- 同 ef 召回 0.955 → 0.916:ef 需随规模增长(ef=120 恢复至 0.943),属 Vamana 已知特性
- 进程扩展近线性(753 → 1,459,1.94×),只要内存装得下
- **磁盘版(sq8,单线程,1.28GB)= 内存版(4 线程,6.4GB)约 50% QPS,内存 1/5**

**HTTP 服务(10M 规模冒烟)**:`/health` → `{"vectors":10000000, "routing_memory_bytes":1280001024}`;
单查询端到端 4.7ms(84 次块读,pread 后端)。生产形态下全精度向量零内存驻留。

## 复现

```bash
# 数据与图
cargo run --release -p nvvec-bench -- vamana --data-dir data/sift --graph g.graph
# 正式基准(Linux + NVMe)
cargo run --release -p nvvec-bench -- pipeline --data-dir data/sift --graph g.graph \
    --index idx --ef 80 --w 4 --concurrency 16 --direct --routing sq8
# HTTP 服务
cargo run --release -p nvvec-server -- --index idx --routing sq8 --codes codes.sq8
```

## 已知限制与下一步

- 调度线程是 CPU 瓶颈(4 核机):SQ8 距离核 SIMD 化、pool 插入去 memmove 化可再提 2-3×
- RaBitQ 1-2bit(M4b)可把路由内存再降 4-8×(精排架构已证明对路由误差高度耐受)
- 束宽 w 与 ef 的最优组合随盘特性变化,未做自动调参
- 在线写入、过滤是二期(见 ROADMAP)
